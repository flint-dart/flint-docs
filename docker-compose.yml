version: '3.8'

services:
  flint_docs:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3000"
    env_file:
      - .env
    environment:
      - FLINT_HOT=0
      - PORT=3000
    restart: unless-stopped
